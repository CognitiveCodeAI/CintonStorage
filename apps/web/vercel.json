{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd api && pnpm install && pnpm exec prisma generate && cd .. && pnpm run build",
  "outputDirectory": "dist",
  "framework": null,
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 10
    }
  },
  "rewrites": [
    {
      "source": "/api/trpc/:path*",
      "destination": "/api"
    },
    {
      "source": "/api/health",
      "destination": "/api"
    }
  ]
}
